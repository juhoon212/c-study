double multiply(double a, double b) {
    return a * b;
}

double devide(double a, double b) {
    return a / b;
}

// 어셈블리 코드가 짧다고 빠른것이 아니다.

// 비트 연산자 혹은 쉬프트 연산자가 일반 연산자보다 빠른 것은 아니다.

// 컴파일러 마다 optimization이 다르다.